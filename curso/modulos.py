import math, sys #es recomendable colocar el import al principio del código, aunque realmente se puede poner en cualquier parte, siempre que sea antes de que usemos
#cualquier entidad del mismo
#podemos importar varios módulos usando varios import o separando por comas
print(math.sin(math.pi/2))

#para acceder a una entidad usaremos el operador de acceso .
#si usamos el . para acceder a una entidad del módulo entonces no habrá conflicto alguno aunque haya una variable o función o cualquier otra entidad en nuestro programa
#que tenga el mismo nombre de la entidad a la que llamamos usando el .
